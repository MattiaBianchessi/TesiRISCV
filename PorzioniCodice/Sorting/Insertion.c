void insertionSort(int a[], int dim)
{

    int temp, j;
    for (int i = 1; i < dim; i++)
    {
        temp = a[i];
        j = i - 1;
        while (j >= 0 && a[j] > temp)
        {
            a[j + 1] = a[j];
            j--;
        }
        a[j + 1] = temp;
    }
}